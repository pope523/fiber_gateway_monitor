{
  "_description": "Reversed range bounds drop the field and WARN — firmware shape signal worth surfacing rather than silently absoluting",
  "_json": {"channels": [{"id": "1", "freq": "860~751"}]},
  "_resource": "/api/data",
  "_config": {
    "format": "json",
    "resource": "/api/data",
    "array_path": "channels",
    "fields": [
      {"key": "id", "field": "channel_id", "type": "integer"},
      {"key": "freq", "field": "channel_width", "type": "frequency", "separator": "~", "range": "span"}
    ]
  },
  "_expected": [
    {"channel_id": 1}
  ],
  "_expected_warnings": ["860~751"]
}
