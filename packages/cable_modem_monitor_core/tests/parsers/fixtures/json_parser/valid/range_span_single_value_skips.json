{
  "_description": "Single-value raw without the separator silently skips — supports mixed-shape tables (SC-QAM rows reading the same Frequency key as OFDM range rows in TG3442DE)",
  "_json": {"channels": [{"id": "1", "freq": "750"}]},
  "_resource": "/api/data",
  "_config": {
    "format": "json",
    "resource": "/api/data",
    "array_path": "channels",
    "fields": [
      {"key": "id", "field": "channel_id", "type": "integer"},
      {"key": "freq", "field": "channel_width", "type": "frequency", "separator": "~", "range": "span"}
    ]
  },
  "_expected": [
    {"channel_id": 1}
  ],
  "_expected_warnings": []
}
