{
  "_description": "range: span computes (high - low) from a separator-split value, producing channel_width in Hz via the frequency type's MHz->Hz normalization",
  "_json": {"channels": [{"id": "1", "freq": "751~860"}]},
  "_resource": "/api/data",
  "_config": {
    "format": "json",
    "resource": "/api/data",
    "array_path": "channels",
    "fields": [
      {"key": "id", "field": "channel_id", "type": "integer"},
      {"key": "freq", "field": "channel_width", "type": "frequency", "separator": "~", "range": "span"}
    ]
  },
  "_expected": [
    {"channel_id": 1, "channel_width": 109000000}
  ]
}
