{
  "_description": "Numeric raw value (e.g. SC-QAM 'Frequency': 570) silently skips — no span to compute, no WARN",
  "_json": {"channels": [{"id": "1", "freq": 570}]},
  "_resource": "/api/data",
  "_config": {
    "format": "json",
    "resource": "/api/data",
    "array_path": "channels",
    "fields": [
      {"key": "id", "field": "channel_id", "type": "integer"},
      {"key": "freq", "field": "channel_width", "type": "frequency", "separator": "~", "range": "span"}
    ]
  },
  "_expected": [
    {"channel_id": 1}
  ],
  "_expected_warnings": []
}
